{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Magic Castle on OVHcloud","text":"<p>A community guide for deploying a SLURM HPC cluster on OVHcloud using Magic Castle.</p> <p>Tested with Magic Castle 12.2.0 on OVHcloud regions GRA and SBG. If you use a different version, some configuration options or behavior may differ \u2014 check the Magic Castle changelog.</p>"},{"location":"#why-this-guide-exists","title":"Why this guide exists","text":"<p>I've been deploying Magic Castle clusters on OVHcloud for years \u2014 for genomics pipelines, bioinformatics platforms, and production HPC workloads. Every time, I hit the same walls: scattered documentation, OVHcloud-specific quirks that nobody writes about, and hours lost to problems that a single paragraph could have prevented.</p> <p>This guide is the result of that accumulated experience. It's the document I wish I'd had on day one.</p> <p>What you'll find here is a mix of two things:</p> <ol> <li> <p>The essentials from official docs, consolidated in one place. Magic Castle, OVHcloud,    OpenStack, Terraform, SLURM \u2014 the relevant bits from each, assembled into a single    coherent walkthrough instead of five browser tabs.</p> </li> <li> <p>The things that aren't in any official doc. The network setup that must happen    before <code>terraform apply</code>. The volume types that silently don't work. The flavor naming    inconsistencies across regions. The SLURM tuning that turns a demo cluster into a    production one. These are lessons learned the hard way, over many deployments and many    hours of debugging.</p> </li> </ol>"},{"location":"#who-this-is-for","title":"Who this is for","text":"<p>If you know SLURM but not Terraform: this guide explains what Terraform does, why Magic Castle uses it, and walks through each configuration step without assuming cloud experience.</p> <p>If you know Terraform but not SLURM: this guide explains the SLURM architecture, what each node type does, and what configuration choices matter for a bioinformatics workload.</p>"},{"location":"#prerequisites-checklist","title":"Prerequisites checklist","text":"<p>Before starting, make sure you have:</p> <ul> <li>[ ] An OVHcloud Public Cloud project (not a dedicated server \u2014 the Public Cloud section)</li> <li>[ ] A project with sufficient quotas (vCPUs, RAM, volumes, floating IPs)</li> <li>[ ] An SSH key pair registered in the OVHcloud console</li> <li>[ ] Terraform \u2265 1.5.7 installed locally</li> <li>[ ] The OpenStack RC File v3 downloaded from your OVHcloud project</li> <li>[ ] Git to clone Magic Castle</li> </ul>"},{"location":"#guide-sections","title":"Guide sections","text":"Section What you will do Prerequisites Account setup, quotas, SSH keys, Terraform install Architecture What nodes exist and why Network setup Create network/subnet/router before running Magic Castle main.tf walkthrough Configure your cluster for OVHcloud OVHcloud specifics What the official docs don't tell you Bioinformatics stack Add Nextflow, Apptainer, and S3FS SLURM tuning Production scheduler configuration Nextflow on Magic Castle Run pipelines on your cluster Deploy and destroy Safe deployment workflow Troubleshooting OVHcloud-specific errors and fixes"},{"location":"#about-the-author","title":"About the author","text":"<p>I'm Christophe Demay, a senior bioinformatics consultant. I've spent years building and operating HPC clusters on OVHcloud for genomics and bioinformatics workloads \u2014 from proof-of-concept setups to production platforms running large-scale Nextflow pipelines.</p> <p>This guide condenses everything I've learned into a single resource: the tips, the workarounds, the configuration choices that actually matter, and the mistakes you don't need to repeat. Every code example and configuration snippet has been written from scratch using publicly available documentation \u2014 no proprietary code or client-specific material is included.</p> <p>I wrote this because the community deserves a straightforward path from zero to a working cluster, without the hours of trial and error I went through.</p>"},{"location":"#quick-links","title":"Quick links","text":"<ul> <li>Magic Castle GitHub</li> <li>Magic Castle documentation</li> <li>OVHcloud Public Cloud</li> <li>OpenStack Terraform provider</li> </ul>"},{"location":"01-prerequisites/","title":"Prerequisites","text":""},{"location":"01-prerequisites/#ovhcloud-account-and-project","title":"OVHcloud account and project","text":"<p>You need an OVHcloud Public Cloud project \u2014 not a VPS or dedicated server. Public Cloud is OVHcloud's OpenStack-based infrastructure.</p> <ol> <li>Go to the OVHcloud Control Panel</li> <li>Create a new Public Cloud project (or use an existing one)</li> <li>Note your Project ID (visible in the URL and project overview)</li> </ol>"},{"location":"01-prerequisites/#quotas","title":"Quotas","text":"<p>OVHcloud projects start with default quotas that are sufficient for a small test cluster. For a production deployment you will likely need to increase them.</p> <p>Default limits (vary by region \u2014 check your project's Quota page):</p> Resource Typical default vCPUs 20 RAM 50 GB Volumes 10 Volume storage 200 GB Floating IPs 1\u20133 Security groups 10 <p>A minimal Magic Castle cluster (1 login + 1 mgmt + 2 compute nodes) uses roughly: 8\u201316 vCPUs, 16\u201332 GB RAM, 3 floating IPs, and 2\u20133 volumes.</p> <p>To request a quota increase: Control Panel \u2192 Public Cloud project \u2192 Quota and Regions \u2192 Increase my quota.</p>"},{"location":"01-prerequisites/#ssh-key-pair","title":"SSH key pair","text":"<p>Magic Castle configures SSH access on all nodes using a key you register in OVHcloud.</p> <pre><code># Generate a key pair if you don't have one\nssh-keygen -t ed25519 -C \"magic-castle\" -f ~/.ssh/mc_ovh\n\n# The public key to register is:\ncat ~/.ssh/mc_ovh.pub\n</code></pre> <p>Register the public key: Control Panel \u2192 Public Cloud \u2192 SSH Keys \u2192 Add a key</p> <p>Note the key name \u2014 you will reference it in <code>main.tf</code>.</p>"},{"location":"01-prerequisites/#terraform","title":"Terraform","text":"<p>Magic Castle requires Terraform \u2265 1.5.7.</p> <pre><code># Check current version\nterraform version\n\n# Install via package manager (Linux)\nsudo apt install terraform         # Debian/Ubuntu (HashiCorp repo required)\nbrew install terraform             # macOS\n\n# Or download directly from HashiCorp\n# https://developer.hashicorp.com/terraform/install\n</code></pre> <p>Note for HPC users: if you are running this from a login node or a shared machine, install Terraform in your <code>$HOME</code> \u2014 it is a single static binary.</p>"},{"location":"01-prerequisites/#openstack-rc-file-v3","title":"OpenStack RC File v3","text":"<p>Magic Castle uses the OpenStack Terraform provider, which authenticates via standard OpenStack environment variables.</p> <p>Download the RC file: Control Panel \u2192 Public Cloud project \u2192 Users &amp; Roles \u2192 your user \u2192 Download OpenStack RC File \u2192 choose Version 3 (v2 will not work).</p> <p>The file looks like:</p> <pre><code>#!/usr/bin/env bash\nexport OS_AUTH_URL=https://auth.cloud.ovh.net/v3\nexport OS_TENANT_ID=&lt;your-tenant-id&gt;\nexport OS_TENANT_NAME=\"&lt;your-project-name&gt;\"\nexport OS_USERNAME=\"&lt;your-username&gt;\"\nexport OS_PASSWORD=\"&lt;your-password&gt;\"\nexport OS_REGION_NAME=\"GRA11\"\nexport OS_INTERFACE=public\nexport OS_IDENTITY_API_VERSION=3\n</code></pre> <p>Source it before running Terraform:</p> <pre><code>source ~/openstack-rc-gra11.rc\n# Enter your password when prompted (unless you hardcoded it \u2014 avoid this)\n</code></pre> <p>Tip: rename the file to include the region name (e.g. <code>openstack-rc-gra11.rc</code>) if you plan to deploy in multiple regions. Sourcing the wrong RC file is a common source of confusing errors.</p>"},{"location":"01-prerequisites/#clone-magic-castle","title":"Clone Magic Castle","text":"<pre><code>git clone https://github.com/ComputeCanada/magic_castle.git\ncd magic_castle/ovh\nls\n# main.tf.example  README.md  ...\n</code></pre> <p>Copy the example config:</p> <pre><code>cp main.tf.example main.tf\n</code></pre> <p>You will edit <code>main.tf</code> extensively \u2014 see main.tf walkthrough.</p>"},{"location":"01-prerequisites/#references","title":"References","text":"<ul> <li>Terraform installation \u2014 download and install Terraform for your platform</li> <li>Magic Castle releases \u2014 pinned release tags for stable deployments</li> <li>OVHcloud Public Cloud getting started \u2014 creating your first Public Cloud project</li> </ul>"},{"location":"02-architecture/","title":"Architecture","text":""},{"location":"02-architecture/#what-magic-castle-deploys","title":"What Magic Castle deploys","text":"<p>A Magic Castle cluster has four types of nodes. Each runs a specific role and is sized differently.</p> <pre><code>Internet\n    \u2502\n    \u25bc\n[login node]  \u2190 SSH entry point, job submission, file transfers\n    \u2502\n    \u251c\u2500\u2500 [mgmt node]   \u2190 Puppet server, LDAP, SLURM controller, DNS\n    \u2502\n    \u2514\u2500\u2500 [compute nodes]  \u2190 Where jobs actually run (1 to N instances)\n\nShared storage: NFS exported from mgmt node (or external volume)\n</code></pre>"},{"location":"02-architecture/#node-roles","title":"Node roles","text":""},{"location":"02-architecture/#login-node-login1","title":"Login node (<code>login1</code>)","text":"<ul> <li>The only node with a public IP (floating IP)</li> <li>Users SSH here to submit jobs, transfer data, edit scripts</li> <li>Runs the SLURM client but not the scheduler</li> <li>Should be sized for interactive work: 2\u20134 vCPUs, 8\u201316 GB RAM</li> <li>Not where compute happens \u2014 jobs run on compute nodes</li> </ul>"},{"location":"02-architecture/#management-node-mgmt1","title":"Management node (<code>mgmt1</code>)","text":"<ul> <li>No public IP \u2014 only reachable from within the cluster</li> <li>Runs: Puppet master, OpenLDAP, SLURM controller (<code>slurmctld</code>), NFS server</li> <li>Critical: if this node is down, job submission stops</li> <li>Sized conservatively: 2\u20134 vCPUs, 4\u20138 GB RAM (it manages, doesn't compute)</li> <li>Exports <code>/home</code> and <code>/project</code> via NFS to all other nodes</li> </ul>"},{"location":"02-architecture/#compute-nodes-node1-n","title":"Compute nodes (<code>node[1-N]</code>)","text":"<ul> <li>No public IP</li> <li>Run SLURM daemon (<code>slurmd</code>)</li> <li>Mount shared filesystems from mgmt node</li> <li>Sized for your workload: genomics pipelines typically need 8\u201332 vCPUs and 32\u2013128 GB RAM</li> <li>You define how many and which flavors in <code>main.tf</code></li> </ul>"},{"location":"02-architecture/#node-naming","title":"Node naming","text":"<p>Magic Castle uses Ansible/Puppet naming conventions. The default names are <code>login1</code>, <code>mgmt1</code>, <code>node1</code>, <code>node2</code>, etc. You can customise them in <code>main.tf</code>.</p>"},{"location":"02-architecture/#storage-options","title":"Storage options","text":"<p>Magic Castle provides two storage approaches on OVHcloud:</p>"},{"location":"02-architecture/#nfs-from-mgmt-node-default","title":"NFS from mgmt node (default)","text":"<p>The mgmt node exports <code>/home</code> and <code>/project</code> (and <code>/scratch</code> if configured) via NFS. All nodes mount these at boot.</p> <ul> <li>Pros: simple, no extra cost, works out of the box</li> <li>Cons: single point of failure, bandwidth limited by mgmt node's disk I/O</li> <li>Good for: small clusters, development, ephemeral workloads</li> </ul>"},{"location":"02-architecture/#attached-volumes-ovhcloud-block-storage","title":"Attached volumes (OVHcloud Block Storage)","text":"<p>Additional persistent volumes can be attached to the mgmt node and exported via NFS, or attached directly to individual nodes.</p> <ul> <li>Classic volumes: standard SATA-backed, cheaper, sufficient for most genomics data</li> <li>High-speed-gen2 volumes: NVMe-backed, ~3\u00d7 faster I/O, use for databases or scratch</li> </ul> <p>See OVHcloud specifics for how to choose and main.tf walkthrough for configuration.</p>"},{"location":"02-architecture/#ovhcloud-object-storage-s3-compatible","title":"OVHcloud Object Storage (S3-compatible)","text":"<p>OVHcloud Object Storage is S3-compatible and can be mounted on the cluster using <code>s3fs</code>. This is the recommended approach for large reference databases and output data that outlive the cluster.</p> <p>See Bioinformatics stack for mounting configuration.</p>"},{"location":"02-architecture/#network-topology","title":"Network topology","text":"<p>All cluster nodes communicate on a private network. Only the login node has a floating IP.</p> <pre><code>OVHcloud Private Network (10.x.x.0/24)\n\u251c\u2500\u2500 login1    10.x.x.10   + floating IP (public)\n\u251c\u2500\u2500 mgmt1     10.x.x.11   (private only)\n\u251c\u2500\u2500 node1     10.x.x.20   (private only)\n\u2514\u2500\u2500 node2     10.x.x.21   (private only)\n</code></pre> <p>You must create this private network before running Magic Castle. See Network setup.</p>"},{"location":"02-architecture/#software-stack","title":"Software stack","text":"<p>Magic Castle configures the cluster using Puppet. On first boot, all nodes pull their configuration from the Puppet master on <code>mgmt1</code>. This takes 10\u201320 minutes.</p> <p>Default software stack includes: - SLURM workload manager - OpenLDAP for user management - FreeIPA (optional) for Kerberos/GSSAPI - JupyterHub (optional) - Lmod environment modules - EESSI software stack (3000+ scientific applications)</p> <p>For bioinformatics workloads, the default stack lacks Nextflow, Apptainer, and S3FS. See Bioinformatics stack for how to add them.</p>"},{"location":"02-architecture/#references","title":"References","text":"<ul> <li>Magic Castle documentation \u2014 official docs covering architecture, configuration, and deployment</li> </ul>"},{"location":"03-network-setup/","title":"Network Setup","text":"<p>This step is mandatory and must be done before running <code>terraform apply</code>.</p> <p>Magic Castle does not create the OVHcloud private network for you \u2014 it expects one to already exist. Skipping this step causes a cryptic Terraform error about missing network resources.</p>"},{"location":"03-network-setup/#what-you-need-to-create","title":"What you need to create","text":"<p>Three interconnected resources in your OVHcloud project:</p> <ol> <li>Private network \u2014 the virtual LAN that all cluster nodes connect to</li> <li>Subnet \u2014 the IP address range within the network (e.g. <code>10.10.0.0/24</code>)</li> <li>Router \u2014 connects the private network to the public internet (required for nodes    to download packages during Puppet configuration)</li> </ol>"},{"location":"03-network-setup/#option-a-create-via-ovhcloud-control-panel-recommended-for-beginners","title":"Option A: Create via OVHcloud Control Panel (recommended for beginners)","text":"<ol> <li>Control Panel \u2192 Public Cloud \u2192 Network \u2192 Private Networks \u2192 Create a private network</li> <li>Name it (e.g. <code>magic-castle-net</code>), select your region, choose a VLAN ID or leave default</li> <li>Configure the subnet: e.g. <code>10.10.0.0/24</code>, gateway <code>10.10.0.1</code>, enable DHCP</li> <li>Go to Routers \u2192 Create a router \u2192 attach it to the private network</li> <li>Add a gateway interface on the router pointing to the external (public) network</li> </ol> <p>Note the network name or ID \u2014 you will reference it in <code>main.tf</code>.</p>"},{"location":"03-network-setup/#option-b-create-via-terraform-infrastructure-as-code-approach","title":"Option B: Create via Terraform (infrastructure-as-code approach)","text":"<p>Create a separate <code>network.tf</code> file alongside your <code>main.tf</code>:</p> <pre><code>terraform {\n  required_providers {\n    openstack = {\n      source  = \"terraform-provider-openstack/openstack\"\n      version = \"~&gt; 1.54\"\n    }\n  }\n}\n\nresource \"openstack_networking_network_v2\" \"cluster_net\" {\n  name           = \"magic-castle-net\"\n  admin_state_up = true\n}\n\nresource \"openstack_networking_subnet_v2\" \"cluster_subnet\" {\n  name            = \"magic-castle-subnet\"\n  network_id      = openstack_networking_network_v2.cluster_net.id\n  cidr            = \"10.10.0.0/24\"\n  ip_version      = 4\n  dns_nameservers = [\"213.186.33.99\", \"8.8.8.8\"]  # OVHcloud DNS + Google fallback\n  # Note: 213.186.33.99 is OVHcloud's standard recursive DNS. If it becomes\n  # unreachable, replace with your region's DNS or use 1.1.1.1 as fallback.\n}\n\nresource \"openstack_networking_router_v2\" \"cluster_router\" {\n  name                = \"magic-castle-router\"\n  external_network_id = data.openstack_networking_network_v2.ext_net.id\n}\n\nresource \"openstack_networking_router_interface_v2\" \"cluster_router_iface\" {\n  router_id = openstack_networking_router_v2.cluster_router.id\n  subnet_id = openstack_networking_subnet_v2.cluster_subnet.id\n}\n\n# Retrieve the external network ID (the public/internet network in your region)\ndata \"openstack_networking_network_v2\" \"ext_net\" {\n  name = \"Ext-Net\"   # This is the standard name on OVHcloud\n}\n</code></pre> <pre><code>terraform init\nterraform apply -target=openstack_networking_network_v2.cluster_net \\\n                -target=openstack_networking_subnet_v2.cluster_subnet \\\n                -target=openstack_networking_router_v2.cluster_router \\\n                -target=openstack_networking_router_interface_v2.cluster_router_iface\n</code></pre> <p>Why a separate apply? Creating network infrastructure before the cluster resources avoids dependency ordering issues and makes it easier to destroy/recreate the cluster without touching the network (useful during development).</p>"},{"location":"03-network-setup/#ovhcloud-specific-the-external-network-name","title":"OVHcloud-specific: the external network name","text":"<p>On OVHcloud, the external network (the one connected to the internet) is always named <code>Ext-Net</code>. This is different from AWS (<code>igw-*</code>) or GCP (<code>default</code>) naming conventions.</p> <p>If the <code>data</code> source lookup fails, list available networks:</p> <pre><code>source openstack-rc-&lt;region&gt;.rc\nopenstack network list\n# Look for the one with \"External\" in its type\n</code></pre>"},{"location":"03-network-setup/#verify-the-network-is-ready","title":"Verify the network is ready","text":"<pre><code>openstack network list\n# Should show your private network\n\nopenstack subnet list\n# Should show your subnet with the CIDR you defined\n\nopenstack router list\n# Should show your router with an external gateway set\n</code></pre> <p>All three must exist before proceeding to <code>main.tf</code> configuration.</p>"},{"location":"03-network-setup/#reusing-an-existing-network-across-deployments","title":"Reusing an existing network across deployments","text":"<p>If you plan to destroy and recreate the cluster (common during setup and testing), keep the network infrastructure in a separate Terraform state or create it manually.</p> <p>This way, you can run <code>terraform destroy</code> on the cluster without destroying the network, and redeploy faster without waiting for the network to be recreated.</p>"},{"location":"03-network-setup/#references","title":"References","text":"<ul> <li>Terraform OpenStack provider \u2014 resource and data source reference for OpenStack networking</li> <li>OVHcloud network concepts \u2014 private networks, subnets, and routers on OVHcloud Public Cloud</li> </ul>"},{"location":"04-main-tf/","title":"main.tf Walkthrough","text":"<p>This section explains each block of a minimal <code>main.tf</code> for an OVHcloud Magic Castle deployment. All values use placeholders \u2014 replace them with your own.</p>"},{"location":"04-main-tf/#provider-configuration","title":"Provider configuration","text":"<pre><code>terraform {\n  required_version = \"&gt;= 1.5.7\"\n  required_providers {\n    openstack = {\n      source  = \"terraform-provider-openstack/openstack\"\n      version = \"~&gt; 1.54\"\n    }\n  }\n}\n</code></pre> <p>Magic Castle on OVHcloud uses the OpenStack provider, not an OVHcloud-specific one. OVHcloud Public Cloud runs on OpenStack, so all standard OpenStack resources apply.</p> <p>No provider credentials go here \u2014 they are picked up from the environment variables set by sourcing your OpenStack RC file.</p>"},{"location":"04-main-tf/#magic-castle-module","title":"Magic Castle module","text":"<pre><code>module \"magic_castle\" {\n  source = \"git::https://github.com/ComputeCanada/magic_castle.git//ovh?ref=12.2.0\"\n\n  config_git_url = \"https://github.com/ComputeCanada/puppet-magic_castle.git\"\n  config_version = \"12.2.0\"\n</code></pre> <p>Always pin to a release tag. Using <code>?ref=main</code> points to the development branch \u2014 a breaking change pushed upstream will silently break your next <code>terraform apply</code>. Pin both <code>source</code> and <code>config_version</code> to the same release tag.</p> <ul> <li><code>source</code>: pulls the OVHcloud-specific Magic Castle Terraform module directly from GitHub.</li> <li><code>config_git_url</code>: the Puppet configuration repository. This is where the software stack   is defined. To add Nextflow, Apptainer, and S3FS, you replace this with a fork.   See Bioinformatics stack.</li> <li><code>config_version</code>: the branch or tag to use in the Puppet repo. Must match the <code>source</code> tag.</li> </ul>"},{"location":"04-main-tf/#cluster-identity","title":"Cluster identity","text":"<pre><code>  cluster_name = \"mycluster\"\n  domain       = \"example.com\"\n</code></pre> <ul> <li><code>cluster_name</code>: becomes the hostname prefix. Nodes will be named <code>mycluster-login1</code>,   <code>mycluster-mgmt1</code>, etc.</li> <li><code>domain</code>: used for internal DNS and SSL certificates. It does not need to be a   registered domain \u2014 it is only used internally.</li> </ul>"},{"location":"04-main-tf/#image","title":"Image","text":"<pre><code>  image = \"Rocky Linux 9\"\n</code></pre> <p>Magic Castle requires a Red Hat-family OS: Rocky Linux, AlmaLinux, or CentOS Stream. Ubuntu and Debian are not supported.</p> <p>On OVHcloud, check available images:</p> <pre><code>openstack image list --public | grep -i rocky\n</code></pre> <p>Use the exact name as it appears in the output.</p> <p>Regional note: image availability varies by region. If your preferred image is not available, check another region or upload your own via Control Panel \u2192 Public Cloud \u2192 Storage \u2192 Object Storage \u2192 Import an image.</p>"},{"location":"04-main-tf/#ssh-key","title":"SSH key","text":"<pre><code>  nb_users     = 10\n  public_keys  = [\"ssh-ed25519 AAAA... your-key-comment\"]\n</code></pre> <ul> <li><code>nb_users</code>: the number of local user accounts to create on the cluster.</li> <li><code>public_keys</code>: list of SSH public keys that will be authorized for all user accounts.   Add your own key here.</li> </ul>"},{"location":"04-main-tf/#instances","title":"Instances","text":"<pre><code>  instances = {\n    login  = { type = \"b2-7\",   tags = [\"login\", \"public\", \"proxy\"], count = 1 }\n    mgmt   = { type = \"b2-7\",   tags = [\"puppet\", \"nfs\", \"slurm-controller\"], count = 1 }\n    node   = { type = \"b2-15\",  tags = [\"node\"], count = 2 }\n  }\n</code></pre>"},{"location":"04-main-tf/#ovhcloud-instance-types-flavors","title":"OVHcloud instance types (flavors)","text":"<p>OVHcloud uses a different naming convention from AWS or GCP:</p> Flavor vCPUs RAM Use case <code>b2-7</code> 2 7 GB Login/mgmt \u2014 interactive work <code>b2-15</code> 4 15 GB Light compute \u2014 small analyses <code>b2-30</code> 8 30 GB Standard genomics workloads <code>b2-60</code> 16 60 GB WGS, multi-sample analyses <code>b2-120</code> 32 120 GB Heavy parallel work <code>r2-30</code> 2 30 GB Memory-intensive (variant calling) <code>r2-60</code> 4 60 GB High-memory analyses <p>Check what is available in your region before writing <code>main.tf</code>:</p> <pre><code>openstack flavor list | grep -E \"^| b2| r2| c2\"\n</code></pre> <p>Friction point: not all flavors are available in all regions. GRA (Gravelines) and SBG (Strasbourg) have the widest selection. If a flavor is unavailable, Terraform will fail with an obscure error about no valid hosts. Pick an alternative flavor and retry.</p>"},{"location":"04-main-tf/#tags","title":"Tags","text":"<p>Tags define what services run on each node:</p> Tag Service <code>login</code> SSH gateway <code>public</code> Gets a floating IP <code>proxy</code> JupyterHub reverse proxy <code>puppet</code> Puppet master <code>nfs</code> NFS server (exports /home, /project) <code>slurm-controller</code> SLURM <code>slurmctld</code> daemon <code>node</code> SLURM compute node (<code>slurmd</code>)"},{"location":"04-main-tf/#volumes","title":"Volumes","text":"<pre><code>  volumes = {\n    nfs = {\n      home    = 50\n      project = 100\n      scratch = 50\n    }\n  }\n</code></pre> <p>Volumes are attached to the <code>nfs</code>-tagged node (mgmt1 by default) and exported to the cluster via NFS.</p> <p>Sizes are in GB. Adjust based on your expected data volume.</p>"},{"location":"04-main-tf/#attaching-a-pre-existing-volume-by-uuid","title":"Attaching a pre-existing volume by UUID","text":"<p>If you have a persistent volume (e.g. a reference database) that should survive cluster destruction and redeployment:</p> <pre><code>  volumes = {\n    nfs = {\n      home    = 50\n      project = 100\n    }\n    node1 = {\n      references = \"&lt;your-volume-uuid&gt;\"   # attach by existing UUID\n    }\n  }\n</code></pre> <p>This is covered in detail in OVHcloud specifics.</p>"},{"location":"04-main-tf/#network","title":"Network","text":"<p>Reference the network you created in Network setup by name:</p> <pre><code>  network = {\n    name = \"magic-castle-net\"\n  }\n</code></pre> <p>Note: Magic Castle also supports a <code>type</code> key for predefined cloud configurations (e.g. <code>\"arbutus\"</code> on Compute Canada). On OVHcloud, always use <code>name</code> to reference the private network you created manually or via Terraform.</p>"},{"location":"04-main-tf/#complete-minimal-example","title":"Complete minimal example","text":"<pre><code>terraform {\n  required_version = \"&gt;= 1.5.7\"\n  required_providers {\n    openstack = {\n      source  = \"terraform-provider-openstack/openstack\"\n      version = \"~&gt; 1.54\"\n    }\n  }\n}\n\nmodule \"magic_castle\" {\n  source = \"git::https://github.com/ComputeCanada/magic_castle.git//ovh?ref=12.2.0\"\n\n  config_git_url = \"https://github.com/ComputeCanada/puppet-magic_castle.git\"\n  config_version = \"12.2.0\"\n\n  cluster_name = \"mycluster\"\n  domain       = \"example.com\"\n  image        = \"Rocky Linux 9\"\n\n  nb_users    = 5\n  public_keys = [\"ssh-ed25519 AAAA... your-key-comment\"]\n\n  instances = {\n    login = { type = \"b2-7\",  tags = [\"login\", \"public\", \"proxy\"], count = 1 }\n    mgmt  = { type = \"b2-7\",  tags = [\"puppet\", \"nfs\", \"slurm-controller\"], count = 1 }\n    node  = { type = \"b2-15\", tags = [\"node\"], count = 2 }\n  }\n\n  volumes = {\n    nfs = {\n      home    = 50\n      project = 100\n    }\n  }\n\n  network = {\n    name = \"magic-castle-net\"\n  }\n}\n\noutput \"login_ip\" {\n  value = module.magic_castle.public_ip\n}\n</code></pre>"},{"location":"04-main-tf/#references","title":"References","text":"<ul> <li>Magic Castle OVH module variables \u2014 input variables and defaults for the OVHcloud module</li> <li>Terraform module sources \u2014 how Terraform resolves Git-based and registry module sources</li> </ul>"},{"location":"05-ovhcloud-specifics/","title":"OVHcloud Specifics","text":"<p>These are the things you will not find in the official Magic Castle or OVHcloud docs, but that will cost you hours if you don't know them.</p>"},{"location":"05-ovhcloud-specifics/#authentication-always-use-rc-file-v3","title":"Authentication: always use RC File v3","text":"<p>The Magic Castle OVHcloud docs mention the RC file but do not warn you that v2 is incompatible. If you download the wrong version:</p> <pre><code>Error: Error creating OpenStack client: Post \"https://auth.cloud.ovh.net/v2.0/tokens\": 404\n</code></pre> <p>Fix: re-download from Control Panel \u2192 Users &amp; Roles \u2192 your user \u2192 Download OpenStack RC File v3.</p> <p>Also: if you have multiple OVHcloud projects, it is easy to source the wrong RC file. Name your RC files explicitly: <code>openstack-rc-project-gra11.rc</code>, <code>openstack-rc-project-sbg5.rc</code>.</p>"},{"location":"05-ovhcloud-specifics/#flavor-availability-varies-by-region","title":"Flavor availability varies by region","text":"<p>Not all instance types are available in every region. If you request <code>b2-120</code> in <code>SBG5</code> and it is only available in <code>GRA11</code>, Terraform fails with:</p> <pre><code>Error: Error creating openstack_compute_instance_v2: ... No valid host was found\n</code></pre> <p>This error gives no indication that the problem is flavor availability.</p> <p>Before writing main.tf, verify your flavors are available in your target region:</p> <pre><code>source openstack-rc-&lt;region&gt;.rc\nopenstack flavor list | grep b2\n</code></pre> <p>If your preferred flavor is unavailable, check an adjacent region (GRA11 \u2194 SBG5). The two largest French regions usually have the widest selection.</p>"},{"location":"05-ovhcloud-specifics/#persistent-volumes","title":"Persistent volumes","text":"<p>By default, all OVHcloud Block Storage volumes attached by Magic Castle are created and destroyed with the cluster. This is fine for scratch and home directories, but you do not want your 500 GB reference genome database deleted every time you rebuild the cluster.</p>"},{"location":"05-ovhcloud-specifics/#attach-an-existing-volume-by-uuid","title":"Attach an existing volume by UUID","text":"<ol> <li>Create the volume manually in the OVHcloud Control Panel (or via Terraform separately)</li> <li>Format and populate it once</li> <li>Note its UUID</li> <li>Reference it in <code>main.tf</code> to attach without recreating:</li> </ol> <pre><code>  volumes = {\n    nfs = {\n      home    = 50\n      project = 100\n    }\n    # Attach an existing volume \u2014 will NOT be destroyed with the cluster\n    persistent_refs = \"&lt;your-volume-uuid&gt;\"\n  }\n</code></pre> <p>Important: the volume must be in the same region and availability zone as your cluster. You cannot attach a GRA11 volume to a SBG5 cluster.</p>"},{"location":"05-ovhcloud-specifics/#volume-types","title":"Volume types","text":"<p>OVHcloud offers two types of Block Storage:</p> Type Performance Price Use case Classic ~400 IOPS Lower Home dirs, project data, logs High Speed gen2 ~3000 IOPS (NVMe) Higher Reference databases, scratch, intensive I/O <p>For most genomics pipelines, classic volumes for <code>/home</code> and <code>/project</code> and a high-speed volume for reference databases is the right balance.</p>"},{"location":"05-ovhcloud-specifics/#multi-region-deployments","title":"Multi-region deployments","text":"<p>If you work across multiple OVHcloud regions (e.g. one cluster in GRA11 for EU data, one in SBG5 for another project), use Terraform workspaces:</p> <pre><code># Create a workspace per region\nterraform workspace new gra11\nterraform workspace new sbg5\n\n# Switch and deploy\nterraform workspace select gra11\nsource openstack-rc-gra11.rc\nterraform apply -var-file=regions/gra11.tfvars\n\nterraform workspace select sbg5\nsource openstack-rc-sbg5.rc\nterraform apply -var-file=regions/sbg5.tfvars\n</code></pre> <p>Keep a <code>regions/</code> directory with per-region variable files:</p> <pre><code># regions/gra11.tfvars\ncluster_name = \"mycluster-gra\"\nimage        = \"Rocky Linux 9\"   # verify exact name per region\nnode_flavor  = \"b2-30\"\n</code></pre> <p>Pitfall: Terraform state is workspace-scoped. Never run <code>terraform destroy</code> without first verifying which workspace is active (<code>terraform workspace show</code>).</p>"},{"location":"05-ovhcloud-specifics/#ovhcloud-object-storage-credentials","title":"OVHcloud Object Storage credentials","text":"<p>OVHcloud Object Storage is S3-compatible but uses its own credentials \u2014 separate from your OpenStack login. You need EC2 credentials (access key + secret key).</p> <p>Generate them:</p> <pre><code>openstack ec2 credentials create\n# Output: access, secret keys\n</code></pre> <p>Or via Control Panel: Users &amp; Roles \u2192 your user \u2192 EC2 Credentials tab.</p> <p>The S3 endpoint for OVHcloud follows this pattern: <pre><code>https://s3.&lt;region&gt;.perf.cloud.ovh.net\n</code></pre> e.g. <code>https://s3.gra.perf.cloud.ovh.net</code> for the GRA region.</p> <p>Never hardcode these credentials in your Terraform files or Puppet configuration. Use environment variables, an encrypted YAML file, or a secrets manager. See Bioinformatics stack for how to pass them securely to the cluster.</p>"},{"location":"05-ovhcloud-specifics/#floating-ip-and-security-groups","title":"Floating IP and security groups","text":"<p>Magic Castle automatically creates a floating IP for the login node. However, if your project quota only allows 1 floating IP and you already have one allocated elsewhere, <code>terraform apply</code> will fail silently (the IP just won't be allocated, and the login node will have no public access).</p> <p>Check before applying:</p> <pre><code>openstack floating ip list\nopenstack quota show | grep floatingip\n</code></pre> <p>Release unused floating IPs in the Control Panel if you are at the limit.</p>"},{"location":"05-ovhcloud-specifics/#puppet-configuration-delay","title":"Puppet configuration delay","text":"<p>After <code>terraform apply</code> completes, the cluster is not immediately ready. Each node bootstraps via Puppet, which takes 15\u201330 minutes depending on the number of nodes and the software stack.</p> <p>You can monitor progress:</p> <pre><code>ssh rocky@&lt;login-ip&gt; -i ~/.ssh/mc_ovh\n# Once connected:\njournalctl -u puppet -f          # watch Puppet runs\nsystemctl status puppet           # check service state\ntail -f /var/log/puppetlabs/puppet/puppet.log\n</code></pre> <p>The cluster is ready when <code>sinfo</code> returns your nodes in <code>idle</code> state:</p> <pre><code>sinfo\n# PARTITION  AVAIL  TIMELIMIT  NODES  STATE  NODELIST\n# cpubase*   up     infinite       2  idle   node[1-2]\n</code></pre> <p>See Deploy and destroy for a script that waits for Puppet to finish.</p>"},{"location":"05-ovhcloud-specifics/#references","title":"References","text":"<ul> <li>OVHcloud API console \u2014 interactive explorer for all OVHcloud API endpoints</li> <li>OpenStack CLI reference \u2014 command reference for the <code>openstack</code> CLI used throughout this guide</li> </ul>"},{"location":"06-bioinfo-stack/","title":"Bioinformatics Software Stack","text":"<p>The default Magic Castle Puppet configuration does not include Nextflow, Apptainer, or S3FS. This section explains how to add them via a custom Puppet fork.</p>"},{"location":"06-bioinfo-stack/#why-a-puppet-fork","title":"Why a Puppet fork","text":"<p>Magic Castle configures software using Puppet and a dedicated configuration repository (<code>puppet-magic_castle</code>). The official repo covers the base cluster (SLURM, LDAP, modules, EESSI) but intentionally stays generic.</p> <p>To add bioinformatics tools, you maintain a fork that extends the official config.</p> <p>The fork approach: 1. Fork <code>https://github.com/ComputeCanada/puppet-magic_castle</code> on GitHub 2. Add your customisations as a new branch (e.g. <code>feature/bioinfo-stack</code>) 3. Point your <code>main.tf</code> to your fork:</p> <pre><code>module \"magic_castle\" {\n  ...\n  config_git_url = \"https://github.com/&lt;your-username&gt;/puppet-magic_castle.git\"\n  config_version = \"feature/bioinfo-stack\"\n}\n</code></pre> <p>Important: the fork must remain compatible with the base Magic Castle version you are using. Check the base repo's release notes when upgrading.</p>"},{"location":"06-bioinfo-stack/#adding-nextflow","title":"Adding Nextflow","text":""},{"location":"06-bioinfo-stack/#what-to-configure","title":"What to configure","text":"<p>Nextflow requires: - Java (OpenJDK 17 or 21) - Nextflow binary itself - Optional: <code>NXF_HOME</code> pointing to a shared directory (avoids re-downloading plugins per user)</p>"},{"location":"06-bioinfo-stack/#in-your-puppet-manifest","title":"In your Puppet manifest","text":"<p>In your fork, add to the relevant profile (e.g. <code>profile::base</code> or a new <code>profile::nextflow</code>):</p> <pre><code># Install Java\npackage { 'java-21-openjdk-headless':\n  ensure =&gt; installed,\n}\n\n# Download and install Nextflow\nexec { 'install_nextflow':\n  command =&gt; '/bin/bash -c \"curl -fsSL https://get.nextflow.io | bash &amp;&amp; mv nextflow /usr/local/bin/ &amp;&amp; chmod +x /usr/local/bin/nextflow\"',\n  creates =&gt; '/usr/local/bin/nextflow',\n  require =&gt; Package['java-21-openjdk-headless'],\n}\n</code></pre> <p>Pin the Nextflow version for reproducibility:</p> <pre><code>exec { 'install_nextflow':\n  command =&gt; '/bin/bash -c \"curl -fsSL https://github.com/nextflow-io/nextflow/releases/download/v&lt;VERSION&gt;/nextflow &gt; /usr/local/bin/nextflow &amp;&amp; chmod +x /usr/local/bin/nextflow\"',\n  creates =&gt; '/usr/local/bin/nextflow',\n}\n</code></pre> <p>Replace <code>&lt;VERSION&gt;</code> with the desired version (e.g. <code>24.10.0</code>).</p>"},{"location":"06-bioinfo-stack/#adding-apptainer-singularity","title":"Adding Apptainer (Singularity)","text":""},{"location":"06-bioinfo-stack/#why-non-suid","title":"Why non-SUID","text":"<p>By default, Apptainer's <code>starter-suid</code> binary is installed with SUID root, which allows unprivileged users to create fakeroot environments. On many HPC clusters, system administrators disable SUID executables for security reasons.</p> <p>For tighter security or if your OVHcloud instances run with SELinux enforcing:</p> <pre><code># Install Apptainer without SUID\npackage { 'apptainer':\n  ensure =&gt; '1.4.5',   # pin version\n}\n\nexec { 'disable_apptainer_suid':\n  command =&gt; '/bin/chmod u-s /usr/libexec/apptainer/bin/starter-suid',\n  onlyif  =&gt; '/bin/test -u /usr/libexec/apptainer/bin/starter-suid',\n  require =&gt; Package['apptainer'],\n}\n</code></pre>"},{"location":"06-bioinfo-stack/#container-cache-on-shared-storage","title":"Container cache on shared storage","text":"<p>Configure Apptainer to use a shared cache directory to avoid each user downloading the same container images independently:</p> <pre><code># In /etc/environment or via Puppet:\nAPPTAINER_CACHEDIR=/project/containers/cache\nAPPTAINER_TMPDIR=/scratch/apptainer_tmp\n</code></pre>"},{"location":"06-bioinfo-stack/#s3fs-mounting-ovhcloud-object-storage","title":"S3FS: mounting OVHcloud Object Storage","text":"<p>S3FS mounts an S3-compatible bucket as a POSIX filesystem. Use it to mount reference databases or large input datasets stored in OVHcloud Object Storage.</p>"},{"location":"06-bioinfo-stack/#install-s3fs-via-puppet","title":"Install S3FS via Puppet","text":"<pre><code>package { 's3fs-fuse':\n  ensure =&gt; installed,\n}\n</code></pre>"},{"location":"06-bioinfo-stack/#s3fs-credentials","title":"S3FS credentials","text":"<p>S3FS reads credentials from <code>~/.passwd-s3fs</code> or <code>/etc/passwd-s3fs</code>. Format:</p> <pre><code>&lt;access-key&gt;:&lt;secret-key&gt;\n</code></pre> <pre><code>echo \"&lt;access-key&gt;:&lt;secret-key&gt;\" &gt; /etc/passwd-s3fs\nchmod 600 /etc/passwd-s3fs\n</code></pre> <p>Never hardcode credentials in your Puppet repository if it is public.</p> <p>Options for secure credential delivery: - Pass as Terraform variables and write via a <code>file</code> Terraform resource - Use Puppet Hiera with eyaml encryption (PKCS7 keys) - Use OVHcloud's native secrets management if available in your region</p>"},{"location":"06-bioinfo-stack/#mount-a-bucket-via-etcfstab","title":"Mount a bucket via /etc/fstab","text":"<pre><code>s3fs#&lt;bucket-name&gt; /mnt/references fuse _netdev,allow_other,use_path_request_style,url=https://s3.gra.perf.cloud.ovh.net,passwd_file=/etc/passwd-s3fs 0 0\n</code></pre> <p>Replace <code>&lt;bucket-name&gt;</code> with your OVHcloud Object Storage container name and adjust the region in the URL.</p>"},{"location":"06-bioinfo-stack/#mount-via-puppet","title":"Mount via Puppet","text":"<pre><code>mount { '/mnt/references':\n  ensure  =&gt; mounted,\n  device  =&gt; 's3fs#&lt;bucket-name&gt;',\n  fstype  =&gt; 'fuse',\n  options =&gt; '_netdev,allow_other,use_path_request_style,url=https://s3.gra.perf.cloud.ovh.net,passwd_file=/etc/passwd-s3fs',\n  require =&gt; [Package['s3fs-fuse'], File['/etc/passwd-s3fs']],\n}\n</code></pre>"},{"location":"06-bioinfo-stack/#mounting-multiple-buckets","title":"Mounting multiple buckets","text":"<p>For multiple buckets (input data, output, reference databases):</p> <pre><code>$s3_mounts = {\n  '/mnt/references' =&gt; '&lt;bucket-references&gt;',\n  '/mnt/inputs'     =&gt; '&lt;bucket-inputs&gt;',\n  '/mnt/outputs'    =&gt; '&lt;bucket-outputs&gt;',\n}\n\n$s3_mounts.each |$mountpoint, $bucket| {\n  file { $mountpoint:\n    ensure =&gt; directory,\n  }\n  mount { $mountpoint:\n    ensure  =&gt; mounted,\n    device  =&gt; \"s3fs#${bucket}\",\n    fstype  =&gt; 'fuse',\n    options =&gt; '_netdev,allow_other,...',\n    require =&gt; [Package['s3fs-fuse'], File[$mountpoint]],\n  }\n}\n</code></pre>"},{"location":"06-bioinfo-stack/#verifying-the-stack","title":"Verifying the stack","text":"<p>After Puppet has converged (see Deploy and destroy):</p> <pre><code># SSH to login node\nssh rocky@&lt;login-ip&gt; -i ~/.ssh/mc_ovh\n\n# Check Nextflow\nnextflow -version\n\n# Check Apptainer\napptainer version\n\n# Check S3 mount\nls /mnt/references/\ndf -h /mnt/references\n</code></pre>"},{"location":"06-bioinfo-stack/#references","title":"References","text":"<ul> <li>Nextflow installation \u2014 official install instructions and requirements</li> <li>Apptainer documentation \u2014 user guide covering installation, configuration, and container usage</li> </ul>"},{"location":"07-slurm-tuning/","title":"SLURM Tuning","text":"<p>The default Magic Castle SLURM configuration is conservative. This section covers production-grade settings for bioinformatics workloads on a cloud cluster.</p>"},{"location":"07-slurm-tuning/#autoscaling-enable-or-disable","title":"Autoscaling: enable or disable?","text":"<p>Magic Castle supports SLURM autoscaling \u2014 nodes are suspended when idle and resumed when a job is submitted. This reduces cloud costs but adds job start latency (2\u20135 minutes for a node to resume on OVHcloud).</p>"},{"location":"07-slurm-tuning/#when-to-disable-autoscaling","title":"When to disable autoscaling","text":"<p>Disable autoscaling (<code>SuspendTime=-1</code>) when: - You need immediate job start (interactive analyses, time-sensitive pipelines) - You pay by the hour and the suspend/resume overhead is not worth the saving - Your cluster runs continuously during a project sprint</p>"},{"location":"07-slurm-tuning/#when-to-keep-autoscaling","title":"When to keep autoscaling","text":"<p>Keep autoscaling when: - The cluster is used intermittently (overnight pipeline runs, weekly analyses) - Cost control is a priority - Job latency of a few minutes is acceptable</p>"},{"location":"07-slurm-tuning/#configuration-in-puppet","title":"Configuration in Puppet","text":"<p>In your Puppet configuration (typically <code>slurm.conf</code> template):</p> <pre><code># Disable autoscaling \u2014 nodes always stay up\nSuspendTime=-1\n\n# Enable autoscaling \u2014 suspend after 5 minutes idle\n# SuspendTime=300\n# ResumeTimeout=600    # how long to wait for a node to resume before marking it down\n</code></pre>"},{"location":"07-slurm-tuning/#scheduler-backfill","title":"Scheduler: backfill","text":"<p>SLURM's default scheduler is FIFO (first in, first out). For a shared cluster with mixed job sizes, backfill scheduling fills gaps left by large jobs with smaller jobs that can complete before the large job's resources are needed.</p> <pre><code>SchedulerType=sched/backfill\nSchedulerParameters=bf_continue,bf_max_job_test=200,bf_resolution=600\n</code></pre> <ul> <li><code>bf_continue</code>: allows backfill to run continuously in the background</li> <li><code>bf_max_job_test</code>: maximum jobs to consider in each backfill cycle</li> <li><code>bf_resolution</code>: time resolution for backfill (in seconds)</li> </ul>"},{"location":"07-slurm-tuning/#multi-factor-priority","title":"Multi-factor priority","text":"<p>The default SLURM priority is FIFO. For a shared cluster where fairness matters (multiple users or projects), enable multi-factor priority (MFPQ):</p> <pre><code>PriorityType=priority/multifactor\nPriorityWeightJobSize=100\nPriorityWeightAge=500\nPriorityWeightPartition=100\nPriorityWeightFairshare=1000\nPriorityDecayHalfLife=7-00:00:00    # jobs decay toward zero priority over 7 days\nPriorityMaxAge=14-00:00:00          # maximum age bonus (2 weeks)\n</code></pre> <p>Key parameters: - Age weight (<code>PriorityWeightAge</code>): rewards jobs that have been waiting longer.   This prevents starvation of smaller jobs behind large ones. - Fairshare weight (<code>PriorityWeightFairshare</code>): penalises users or groups that   have used more than their fair share of resources recently.</p>"},{"location":"07-slurm-tuning/#partition-configuration","title":"Partition configuration","text":"<p>Magic Castle creates a single default partition. For bioinformatics workloads, splitting into multiple partitions by resource type avoids misuse:</p> <pre><code>PartitionName=cpubase    Nodes=node[1-4]  Default=YES  MaxTime=7-00:00:00  State=UP\nPartitionName=highmem    Nodes=node[5-6]  Default=NO   MaxTime=3-00:00:00  State=UP\nPartitionName=debug      Nodes=node1      Default=NO   MaxTime=00:30:00    State=UP\n</code></pre> <ul> <li><code>cpubase</code>: standard compute nodes, default partition for most jobs</li> <li><code>highmem</code>: high-memory nodes (r2-series flavors) for variant calling or assembly</li> <li><code>debug</code>: single node, short time limit \u2014 for testing scripts quickly</li> </ul>"},{"location":"07-slurm-tuning/#in-puppet","title":"In Puppet","text":"<p>SLURM partition configuration is managed via the Magic Castle Puppet module. Override in your fork's Hiera data:</p> <pre><code># data/common.yaml\nslurm::partitions:\n  cpubase:\n    nodes: \"node[1-4]\"\n    default: true\n    max_time: \"7-00:00:00\"\n  highmem:\n    nodes: \"node[5-6]\"\n    default: false\n    max_time: \"3-00:00:00\"\n</code></pre>"},{"location":"07-slurm-tuning/#resource-limits","title":"Resource limits","text":"<p>Prevent single users from monopolising the cluster:</p> <pre><code># Global limits\nMaxJobsPerUser=50\nMaxSubmitJobsPerUser=200\n\n# Per-partition limits (in partition definition)\nPartitionName=cpubase  MaxCPUsPerUser=32  MaxMemPerUser=128G\n</code></pre>"},{"location":"07-slurm-tuning/#ntp-configuration","title":"NTP configuration","text":"<p>SLURM requires time synchronisation between nodes. The default Magic Castle config uses standard public NTP servers. If your organisation requires specific NTP servers (e.g. for compliance or low latency), configure them in Puppet:</p> <pre><code># /etc/chrony.conf or via Puppet\n$ntp_servers = ['0.fr.pool.ntp.org', '1.fr.pool.ntp.org']\n</code></pre>"},{"location":"07-slurm-tuning/#monitoring-and-debugging","title":"Monitoring and debugging","text":"<pre><code># Node state\nsinfo -o \"%N %T %C\"    # node, state, CPUs (allocated/idle/other/total)\n\n# Queue state\nsqueue -o \"%.10i %.9P %.30j %.8u %.8T %.10M %.6D %R\"\n\n# Job accounting\nsacct -u &lt;username&gt; --format=JobID,JobName,Elapsed,State,MaxRSS\n\n# SLURM log\njournalctl -u slurmctld -n 100 -f   # on mgmt node\njournalctl -u slurmd -n 100 -f      # on compute nodes\n</code></pre>"},{"location":"07-slurm-tuning/#references","title":"References","text":"<ul> <li>SLURM configuration (slurm.conf) \u2014 full reference for all slurm.conf parameters</li> <li>SLURM priority/multifactor \u2014 how SLURM calculates job priority using multiple factors</li> </ul>"},{"location":"08-nextflow/","title":"Nextflow on Magic Castle","text":"<p>This section covers configuring Nextflow to submit jobs to the SLURM cluster deployed by Magic Castle, and staging data from OVHcloud Object Storage.</p>"},{"location":"08-nextflow/#slurm-executor-configuration","title":"SLURM executor configuration","text":"<p>Create a <code>nextflow.config</code> in your pipeline or home directory:</p> <pre><code>process {\n    executor = 'slurm'\n    queue    = 'cpubase'         // matches your SLURM partition name\n    clusterOptions = '--account=default'\n}\n\nexecutor {\n    name      = 'slurm'\n    queueSize = 50               // max concurrent jobs submitted to SLURM\n    pollInterval = '30 sec'      // how often Nextflow checks job status\n    submitRateLimit = '10/1min'  // max job submissions per minute (be kind to slurmctld)\n}\n</code></pre>"},{"location":"08-nextflow/#per-process-resource-requests","title":"Per-process resource requests","text":"<pre><code>process {\n    executor = 'slurm'\n    queue    = 'cpubase'\n\n    withLabel: 'small' {\n        cpus   = 2\n        memory = '8 GB'\n        time   = '2h'\n    }\n    withLabel: 'medium' {\n        cpus   = 8\n        memory = '32 GB'\n        time   = '12h'\n    }\n    withLabel: 'highmem' {\n        cpus   = 4\n        memory = '64 GB'\n        time   = '24h'\n        queue  = 'highmem'       // send to the high-memory partition\n    }\n}\n</code></pre>"},{"location":"08-nextflow/#apptainer-singularity-integration","title":"Apptainer (Singularity) integration","text":"<p>With Apptainer installed on the cluster (see Bioinformatics stack), Nextflow can run each process inside a container automatically:</p> <pre><code>apptainer {\n    enabled    = true\n    autoMounts = true\n    cacheDir   = '/project/containers/cache'\n}\n</code></pre> <ul> <li><code>autoMounts = true</code>: automatically mounts <code>/home</code>, <code>/project</code>, <code>/scratch</code> inside   containers \u2014 avoids the common error of \"file not found\" inside a container.</li> <li><code>cacheDir</code>: shared cache so containers are pulled once and reused by all users.</li> </ul>"},{"location":"08-nextflow/#using-a-container-per-process","title":"Using a container per process","text":"<pre><code>process ALIGN {\n    container 'docker://quay.io/biocontainers/bwa-mem2:2.2.1--he513fc3_0'\n    ...\n}\n</code></pre> <p>Nextflow pulls the Docker image and converts it to an Apptainer <code>.sif</code> file on first use.</p>"},{"location":"08-nextflow/#data-staging-from-ovhcloud-object-storage","title":"Data staging from OVHcloud Object Storage","text":""},{"location":"08-nextflow/#using-s3-as-pipeline-inputoutput","title":"Using S3 as pipeline input/output","text":"<p>If your reference data and input files are in OVHcloud Object Storage (mounted via S3FS or accessed directly), configure Nextflow's S3 support.</p> <p>Set credentials via environment variables (never hardcode them in config files):</p> <pre><code>export AWS_ACCESS_KEY_ID=&lt;your-ovhcloud-s3-access-key&gt;\nexport AWS_SECRET_ACCESS_KEY=&lt;your-ovhcloud-s3-secret-key&gt;\n</code></pre> <p>Nextflow picks these up automatically. Then configure the S3 endpoint in <code>nextflow.config</code>:</p> <pre><code>aws {\n    client {\n        endpoint = 'https://s3.gra.perf.cloud.ovh.net'\n        s3PathStyleAccess = true   // required for OVHcloud \u2014 path-style, not virtual-hosted\n    }\n}\n</code></pre> <p>Do not put <code>accessKey</code> / <code>secretKey</code> in <code>nextflow.config</code>. Config files end up in version control, shared directories, and Nextflow's <code>.nextflow.log</code>. Environment variables keep credentials out of all three.</p>"},{"location":"08-nextflow/#s3-as-working-directory","title":"S3 as working directory","text":"<pre><code>workDir = 's3://&lt;bucket-name&gt;/nextflow-work'\n</code></pre> <p>This stores intermediate files in Object Storage instead of local disk \u2014 useful for large pipelines where local scratch space is insufficient. Adds latency but removes disk pressure.</p>"},{"location":"08-nextflow/#mixed-local-s3","title":"Mixed local + S3","text":"<p>The most common pattern for large genomics workloads:</p> <pre><code>// Input: from S3 (reference data already there)\n// Work: local NFS scratch (fast I/O during pipeline)\n// Output: push back to S3\n\nparams {\n    genome   = 's3://&lt;references-bucket&gt;/GRCh38/genome.fa'\n    outdir   = 's3://&lt;results-bucket&gt;/run-20240115'\n}\n\nworkDir = '/scratch/nextflow-work'  // local NFS\n</code></pre>"},{"location":"08-nextflow/#slurm-job-naming-and-accounting","title":"SLURM job naming and accounting","text":"<p>Nextflow submits each process as a SLURM job. By default, job names are long and hard to read in <code>squeue</code>. Add a job name prefix:</p> <pre><code>process {\n    executor       = 'slurm'\n    clusterOptions = \"--job-name=nf-${task.process.tokenize(':').last()}\"\n}\n</code></pre>"},{"location":"08-nextflow/#job-tracking","title":"Job tracking","text":"<pre><code># See all Nextflow jobs currently running\nsqueue -u &lt;your-username&gt;\n\n# Past job details (after pipeline finishes)\nsacct -u &lt;your-username&gt; --format=JobID,JobName,Elapsed,MaxRSS,State --starttime=2024-01-01\n</code></pre>"},{"location":"08-nextflow/#profiles-for-reproducibility","title":"Profiles for reproducibility","text":"<p>Define named profiles in <code>nextflow.config</code> to switch between local testing and cluster execution without editing config files:</p> <pre><code>profiles {\n\n    standard {\n        process.executor = 'local'\n    }\n\n    cluster {\n        process.executor   = 'slurm'\n        process.queue      = 'cpubase'\n        apptainer.enabled  = true\n        apptainer.autoMounts = true\n        executor.queueSize = 50\n    }\n\n    test {\n        process.executor = 'local'\n        params.input     = 'test_data/'\n        params.genome    = 'test_data/genome_chr22.fa'\n    }\n\n}\n</code></pre> <p>Run on the cluster:</p> <pre><code>nextflow run main.nf -profile cluster -params-file params.yaml\n</code></pre>"},{"location":"08-nextflow/#common-issues","title":"Common issues","text":""},{"location":"08-nextflow/#jobs-submitted-but-never-start","title":"Jobs submitted but never start","text":"<p>Check SLURM queue and node state: <pre><code>squeue            # are jobs PENDING or RUNNING?\nsinfo             # are nodes idle, drained, or down?\nscontrol show node node1  # detailed node state\n</code></pre></p>"},{"location":"08-nextflow/#container-not-found","title":"Container not found","text":"<p>If Apptainer cannot pull a container: <pre><code># Check disk space on cacheDir\ndf -h /project/containers/cache\n\n# Pull manually to see the error\napptainer pull docker://quay.io/biocontainers/&lt;tool&gt;:&lt;tag&gt;\n</code></pre></p>"},{"location":"08-nextflow/#pipeline-stalls-after-terraform-apply","title":"Pipeline stalls after Terraform apply","text":"<p>Nextflow was likely started before Puppet finished configuring SLURM. Wait for <code>sinfo</code> to show nodes in <code>idle</code> state before submitting pipelines.</p>"},{"location":"08-nextflow/#references","title":"References","text":"<ul> <li>Nextflow SLURM executor \u2014 configuring Nextflow to submit jobs via SLURM</li> <li>Nextflow Apptainer integration \u2014 running pipeline processes inside Apptainer containers</li> </ul>"},{"location":"09-deployment/","title":"Deploy and Destroy","text":"<p>A safe workflow for deploying, monitoring, and destroying your Magic Castle cluster.</p>"},{"location":"09-deployment/#first-deployment","title":"First deployment","text":"<pre><code># 1. Source OpenStack credentials\nsource openstack-rc-gra11.rc\n\n# 2. Initialise Terraform (downloads providers and Magic Castle module)\nterraform init\n\n# 3. Preview what will be created\nterraform plan\n\n# 4. Apply \u2014 this creates all cloud resources\nterraform apply\n# Type 'yes' when prompted\n</code></pre> <p><code>terraform apply</code> completes in 5\u201310 minutes and prints the login node's public IP. The cluster is not yet ready \u2014 Puppet still needs to run on each node.</p>"},{"location":"09-deployment/#waiting-for-puppet-to-finish","title":"Waiting for Puppet to finish","text":"<p>After <code>terraform apply</code>, SSH to the login node and wait for Puppet to converge:</p> <pre><code>LOGIN_IP=$(terraform output -raw login_ip)\n\n# Wait for SSH to become available (may take 2\u20135 minutes after apply)\nuntil ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 rocky@$LOGIN_IP -i ~/.ssh/mc_ovh \"echo ok\" 2&gt;/dev/null; do\n    echo \"Waiting for SSH...\"\n    sleep 30\ndone\n\necho \"SSH ready. Connecting...\"\n</code></pre> <p>Once connected, monitor Puppet:</p> <pre><code># Watch the Puppet log in real time\njournalctl -u puppet -f\n\n# Or check when the last Puppet run completed\npuppet agent --last_run_summary\n</code></pre> <p>Puppet is done when all nodes appear in <code>sinfo</code>:</p> <pre><code>sinfo\n# PARTITION  AVAIL  TIMELIMIT  NODES  STATE  NODELIST\n# cpubase*   up     infinite       2  idle   node[1-2]\n</code></pre>"},{"location":"09-deployment/#deployment-checklist","title":"Deployment checklist","text":"<p>After Puppet converges, verify:</p> <pre><code># SLURM is functional\nsinfo\nsqueue\n\n# Shared filesystems are mounted on all nodes\nssh node1 df -h | grep -E \"home|project|scratch\"\n\n# Software stack is installed (if using bioinformatics Puppet fork)\nwhich nextflow &amp;&amp; nextflow -version\napptainer version\n\n# S3 mounts are accessible (if configured)\nls /mnt/references/\n</code></pre> <p>Submit a test job to confirm end-to-end function:</p> <pre><code>sbatch --wrap \"hostname &amp;&amp; sleep 10 &amp;&amp; echo done\" --output /home/rocky/test-%j.out\nwatch squeue\ncat /home/rocky/test-&lt;jobid&gt;.out\n</code></pre>"},{"location":"09-deployment/#deploying-in-multiple-regions","title":"Deploying in multiple regions","text":"<p>Use Terraform workspaces to manage separate clusters per region:</p> <pre><code># List workspaces\nterraform workspace list\n\n# Create and switch to a region workspace\nterraform workspace new gra11\nterraform workspace select gra11\nsource openstack-rc-gra11.rc\nterraform apply -var-file=regions/gra11.tfvars\n\n# Always verify active workspace before any destructive operation\nterraform workspace show\n</code></pre>"},{"location":"09-deployment/#scaling-compute-nodes","title":"Scaling compute nodes","text":"<p>To add or remove compute nodes without destroying the cluster, update the <code>count</code> in <code>main.tf</code> and apply:</p> <pre><code>instances = {\n  node = { type = \"b2-30\", tags = [\"node\"], count = 5 }  # was 2\n}\n</code></pre> <pre><code>terraform apply\n</code></pre> <p>New nodes will bootstrap via Puppet automatically. Existing nodes are not affected.</p> <p>Before scaling down: drain the nodes gracefully so running jobs are not killed. <pre><code>scontrol update nodename=node[4-5] state=drain reason=\"removing\"\n# Wait for running jobs to finish\nsinfo  # nodes should show 'drained' not 'allocated'\n</code></pre> Then apply the reduced count.</p>"},{"location":"09-deployment/#suspend-and-resume-the-cluster","title":"Suspend and resume the cluster","text":"<p>If you need to pause work (weekend, end of project sprint) but want to keep the cluster state, stop the compute instances:</p> <pre><code># Suspend compute nodes (stop billing for compute, keep volumes and login)\nopenstack server stop node1 node2 node3 node4\n\n# Resume\nopenstack server start node1 node2 node3 node4\n</code></pre> <p>Note: you continue paying for attached volumes and the floating IP while suspended.</p>"},{"location":"09-deployment/#terraform-state-management","title":"Terraform state management","text":"<p>Terraform tracks every cloud resource it creates in a state file (<code>terraform.tfstate</code>). If you lose this file, Terraform no longer knows what it deployed \u2014 you end up with orphaned resources (VMs, volumes, floating IPs) still running and billing, with no way to destroy them cleanly.</p>"},{"location":"09-deployment/#back-up-the-state","title":"Back up the state","text":"<p>At a minimum, copy your state file after every <code>apply</code> or <code>destroy</code>:</p> <pre><code>cp terraform.tfstate terraform.tfstate.backup.$(date +%Y%m%d-%H%M%S)\n</code></pre>"},{"location":"09-deployment/#use-a-remote-backend-recommended-for-teams","title":"Use a remote backend (recommended for teams)","text":"<p>For shared clusters or any deployment you cannot afford to lose, store the state in a remote backend with locking:</p> <pre><code>terraform {\n  backend \"s3\" {\n    bucket   = \"my-tfstate-bucket\"\n    key      = \"magic-castle/terraform.tfstate\"\n    region   = \"gra\"\n    endpoint = \"https://s3.gra.perf.cloud.ovh.net\"\n\n    skip_credentials_validation = true\n    skip_region_validation      = true\n    skip_metadata_api_check     = true\n  }\n}\n</code></pre> <p>This uses OVHcloud Object Storage as an S3-compatible backend. Benefits:</p> <ul> <li>No local file to lose \u2014 state is stored remotely and survives laptop failures</li> <li>Locking \u2014 prevents two people from running <code>terraform apply</code> simultaneously</li> <li>Versioning \u2014 if you enable bucket versioning, you can roll back to a previous state</li> </ul> <p>If you already lost your state: the resources still exist in OVHcloud. List them with <code>openstack server list</code>, <code>openstack volume list</code>, <code>openstack floating ip list</code>, and delete them manually via the CLI or the OVHcloud Control Panel.</p>"},{"location":"09-deployment/#destroying-the-cluster","title":"Destroying the cluster","text":"<pre><code># Always verify which workspace you are in first\nterraform workspace show\n\nterraform destroy\n# Type 'yes' when prompted\n</code></pre> <p>This destroys all cluster resources including volumes created by Magic Castle. Volumes you attached by UUID (pre-existing volumes) are not destroyed \u2014 only the attachment is removed.</p> <p>Data protection: before destroying, make sure any important data is: - Pushed to OVHcloud Object Storage - Stored on a pre-existing volume (attached by UUID, not created by Magic Castle) - Downloaded locally</p> <p>If destruction hangs (common with security groups or floating IPs that have dependencies):</p> <pre><code># Force-remove the stuck resource\nterraform state list                          # find the resource name\nterraform destroy -target=&lt;resource_address&gt;  # destroy just that resource\nterraform destroy                             # retry the full destroy\n</code></pre>"},{"location":"09-deployment/#references","title":"References","text":"<ul> <li>Terraform CLI commands \u2014 reference for init, plan, apply, destroy, and state management</li> <li>Terraform S3 backend \u2014 configuring remote state storage with an S3-compatible backend</li> </ul>"},{"location":"10-troubleshooting/","title":"Troubleshooting","text":"<p>Common errors on OVHcloud Magic Castle deployments, with their causes and fixes.</p>"},{"location":"10-troubleshooting/#terraform-errors","title":"Terraform errors","text":""},{"location":"10-troubleshooting/#error-post-httpsauthcloudovhnetv20tokens-404","title":"<code>Error: Post \"https://auth.cloud.ovh.net/v2.0/tokens\": 404</code>","text":"<p>Cause: you are using an OpenStack RC File v2 instead of v3.</p> <p>Fix: re-download the RC file from Control Panel \u2192 Users &amp; Roles \u2192 Download OpenStack RC File v3.</p>"},{"location":"10-troubleshooting/#error-no-valid-host-was-found","title":"<code>Error: No valid host was found</code>","text":"<p>Cause: the instance flavor you requested is not available in your region.</p> <p>Fix: <pre><code>openstack flavor list | grep b2\n# Check which flavors exist in your current region\n</code></pre> Adjust <code>type</code> in <code>instances</code> block of <code>main.tf</code> to a flavor that is actually available.</p>"},{"location":"10-troubleshooting/#error-error-creating-openstack_networking_floatingip_v2","title":"<code>Error: Error creating openstack_networking_floatingip_v2</code>","text":"<p>Cause: your project has reached the floating IP quota (typically 1\u20133 by default).</p> <p>Fix: <pre><code>openstack floating ip list\n# Release any unused floating IPs\nopenstack floating ip delete &lt;ip-id&gt;\n</code></pre> Or request a quota increase in the Control Panel.</p>"},{"location":"10-troubleshooting/#error-error-creating-openstack_networking_network_v2-already-exists","title":"<code>Error: Error creating openstack_networking_network_v2: ... already exists</code>","text":"<p>Cause: a network with the same name already exists in your project.</p> <p>Fix: use a unique network name in <code>main.tf</code>, or import the existing network into Terraform state: <pre><code>terraform import openstack_networking_network_v2.cluster_net &lt;network-id&gt;\n</code></pre></p>"},{"location":"10-troubleshooting/#terraform-destroy-hangs-on-security-groups","title":"<code>terraform destroy</code> hangs on security groups","text":"<p>Cause: OVHcloud security groups cannot be deleted while instances that use them still exist (or are in a partially-deleted state).</p> <p>Fix: <pre><code># List stuck resources\nterraform state list\n\n# Force-destroy instances first\nterraform destroy -target=module.magic_castle.openstack_compute_instance_v2.instances\n\n# Then retry the full destroy\nterraform destroy\n</code></pre></p>"},{"location":"10-troubleshooting/#cluster-connectivity","title":"Cluster connectivity","text":""},{"location":"10-troubleshooting/#ssh-connection-refused-after-terraform-apply","title":"SSH connection refused after <code>terraform apply</code>","text":"<p>Cause: the login node is still booting. It takes 2\u20135 minutes after <code>apply</code> completes.</p> <p>Fix: wait and retry. If it persists after 10 minutes: <pre><code>openstack server list\nopenstack server show &lt;login-node-id&gt;   # check ACTIVE status and console log\nopenstack console log show &lt;login-node-id&gt;\n</code></pre></p>"},{"location":"10-troubleshooting/#node-shows-as-down-or-drained-in-sinfo","title":"Node shows as <code>down</code> or <code>drained</code> in <code>sinfo</code>","text":"<p>Cause: SLURM daemon (<code>slurmd</code>) failed to start, or Puppet hasn't finished yet.</p> <p>Fix: <pre><code># On the affected node (via login node)\nssh node1\nsystemctl status slurmd\njournalctl -u slurmd -n 50\n\n# If slurmd is stopped, start it\nsystemctl start slurmd\n\n# If the node is drained manually, undrain it\n# (from login node or mgmt node)\nscontrol update nodename=node1 state=resume\n</code></pre></p>"},{"location":"10-troubleshooting/#puppet-never-finishes","title":"Puppet never finishes","text":"<p>Cause: Puppet master on mgmt1 is unreachable (DNS issue, mgmt node not up yet), or the Puppet repository URL is wrong.</p> <p>Fix: <pre><code># On login node, check connectivity to mgmt1\nping mgmt1\nssh mgmt1\n\n# Check Puppet master service\nssh mgmt1 systemctl status puppetserver\n\n# Check the Puppet repo URL matches config_git_url in main.tf\nssh mgmt1 grep git /etc/puppetlabs/puppet/hiera.yaml\n</code></pre></p>"},{"location":"10-troubleshooting/#storage-issues","title":"Storage issues","text":""},{"location":"10-troubleshooting/#s3fs-mount-fails-silently","title":"S3FS mount fails silently","text":"<p>Cause: wrong credentials, wrong endpoint URL, or wrong bucket name.</p> <p>Debug: <pre><code># Unmount and remount with debug output\nfusermount -u /mnt/references\ns3fs &lt;bucket-name&gt; /mnt/references \\\n  -o passwd_file=/etc/passwd-s3fs \\\n  -o use_path_request_style \\\n  -o url=https://s3.gra.perf.cloud.ovh.net \\\n  -o dbglevel=info \\\n  -f    # run in foreground to see errors\n</code></pre></p> <p>Common mistakes: - Using virtual-hosted-style URL (<code>bucket.s3.region...</code>) instead of path-style - Wrong region in the endpoint URL - Credentials generated for a different OVHcloud project or user</p>"},{"location":"10-troubleshooting/#home-not-mounted-on-compute-nodes","title":"<code>/home</code> not mounted on compute nodes","text":"<p>Cause: NFS server (mgmt1) is not running, or the compute node failed to mount at boot.</p> <p>Fix: <pre><code># On mgmt1: verify NFS is exporting\nshowmount -e mgmt1\n\n# On compute node: force remount\nmount -a\n# or\nmount mgmt1:/home /home\n</code></pre></p>"},{"location":"10-troubleshooting/#slurm-issues","title":"SLURM issues","text":""},{"location":"10-troubleshooting/#jobs-stay-pending-indefinitely","title":"Jobs stay <code>PENDING</code> indefinitely","text":"<p>Common reasons:</p> <pre><code># Check the reason column\nsqueue -o \"%.10i %.8T %R\"\n\n# Reasons and fixes:\n# Resources       \u2192 not enough CPUs/memory available; check sinfo\n# ReqNodeNotAvail \u2192 requested node is down; check sinfo\n# Priority        \u2192 waiting for higher-priority jobs; normal, wait\n# QOSMaxCpuPerUserLimit \u2192 per-user CPU limit reached; reduce concurrency\n</code></pre>"},{"location":"10-troubleshooting/#nextflow-pipeline-stalls-jobs-never-submitted","title":"Nextflow pipeline stalls \u2014 jobs never submitted","text":"<p>Cause: Nextflow launched before SLURM was ready (Puppet still running).</p> <p>Fix: <pre><code># Verify SLURM is accepting jobs\nsinfo\nsrun --pty hostname   # interactive test job\n\n# If sinfo shows no nodes, Puppet hasn't finished yet\n# Wait and recheck\n</code></pre></p>"},{"location":"10-troubleshooting/#cost-surprises","title":"Cost surprises","text":""},{"location":"10-troubleshooting/#unexpected-billing-after-cluster-destroy","title":"Unexpected billing after cluster destroy","text":"<p>Floating IPs and volumes may survive <code>terraform destroy</code> if Terraform failed to delete them.</p> <p>Check for orphaned resources: <pre><code>openstack floating ip list\nopenstack volume list\nopenstack server list\n</code></pre></p> <p>Delete any resources that should have been removed. OVHcloud bills for floating IPs and volumes even when not attached to any instance.</p>"}]}